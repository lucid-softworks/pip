node_modules/
.expo/
dist/
web-build/

# Native — CNG: prebuild regenerates these from app.json on demand.
# Run `pnpm exec expo prebuild` (or just `pnpm ios` / `pnpm android` which
# auto-prebuilds when the folder is missing).
/ios
/android

# macOS
.DS_Store

# Env
.env
.env.local
.env.*.local

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Editor
.vscode/
.idea/
